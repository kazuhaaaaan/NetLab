# @mikrolab/cli

> NetLab Lexer, Parser, & AST Command Engine

Grammar lexer, token stream generator, AST parser, and command object compiler converting vendor syntax strings into normalized engine actions.

## Responsibilities
- Module specific execution
- Clean Architecture interface adherence
